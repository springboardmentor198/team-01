import { LuActivity } from "react-icons/lu";
const relativeTime = (value) => { if (!value) return "Time unavailable"; const seconds = Math.max(0, (Date.now() - new Date(value).getTime()) / 1000); if (seconds < 60) return "Just now"; if (seconds < 3600) return `${Math.floor(seconds / 60)} min ago`; if (seconds < 86400) return `${Math.floor(seconds / 3600)} hr ago`; return `${Math.floor(seconds / 86400)} days ago`; };
function RecentActivity({ activities = [] }) { return <article className="admin-dashboard-card"><div className="admin-dashboard-card-header"><div><h2 className="admin-dashboard-card-title">Recent Activity</h2><p className="admin-dashboard-card-subtitle">Latest recorded platform activity</p></div></div>{activities.length === 0 ? <div className="admin-dashboard-empty">No activity has been recorded yet.</div> : <div className="admin-recent-activity">{activities.map((activity) => <div className="admin-activity-item" key={activity.id}><div className="admin-activity-icon" style={{ background: "#EDE9FE", color: "#7C3AED" }}><LuActivity size={17}/></div><div className="admin-activity-info"><p className="admin-activity-title">{activity.title}</p><p className="admin-activity-description">{activity.description}</p></div><span className="admin-activity-time">{relativeTime(activity.createdAt)}</span></div>)}</div>}</article>; }
export default RecentActivity;
