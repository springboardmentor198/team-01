import { LuBuilding2, LuClock3, LuMapPin } from "react-icons/lu";
const relativeTime = (value) => !value ? "Time unavailable" : `${Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 60000))} min ago`;
function RecentPropertyApprovals({ properties = [] }) { return <article className="admin-dashboard-card"><div className="admin-dashboard-card-header"><div><h2 className="admin-dashboard-card-title">Recent Property Approvals</h2><p className="admin-dashboard-card-subtitle">Latest property listings requiring review</p></div></div>{properties.length === 0 ? <div className="admin-dashboard-empty">No property submissions yet.</div> : <div className="admin-property-list">{properties.map((property) => <div className="admin-property-item" key={property.id}><div className="admin-property-image" style={{ display: "flex", alignItems: "center", justifyContent: "center", background: "#EEF2FF", color: "#4F46E5" }}><LuBuilding2 size={21}/></div><div><p className="admin-property-name">{property.name}</p><p className="admin-property-location"><LuMapPin size={10}/> {property.location}</p><p style={{ margin: "4px 0 0", color: "#94A3B8", fontSize: 9 }}>Submitted by {property.submittedBy}</p></div><div className="admin-property-meta"><span className="admin-status-badge pending">{property.status}</span><span className="admin-property-time"><LuClock3 size={10}/> {relativeTime(property.submittedAt)}</span></div></div>)}</div>}</article>; }
export default RecentPropertyApprovals;
